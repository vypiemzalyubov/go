// Замените нижние подчеркивания таким образом, чтобы программа считывала два целых числа со входа и выводила их сумму.

package main

import "fmt"

func main() {
	var x int
	var y int
	fmt.Scan(&x)
	fmt.Scan(&y)
	fmt.Println(x + y)
}
